/*
 * @Description: 联邦共享层 — 统一出口
 * Monorepo 用路径引用，独立仓库用 npm 包引用：
 *   import { MF_APP_NAMES, buildRemoteEntry } from '@federation/shared'
 */

export * from './constants'
export * from './types'
export * from './utils'
